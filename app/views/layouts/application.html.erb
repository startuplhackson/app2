<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:title) %></title>
   <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial scale 1.0">

  		<%= javascript_include_tag "application" %>
  		<link href="/bootstrap/css/bootstrap.css" rel="stylesheet" type="text/css" media="screen">
		<link href="/bootstrap/css/bootstrap-responsive.css" rel="stylesheet" type="text/css" media="screen"> 
		
		<link href="/custom.css" rel="stylesheet" type="text/css" media="screen">
		
		
		<%= csrf_meta_tags %>
</head>
<body>
<div class="container-fluid">
	<h3><strong><%= link_to "TechRumors", root_path %></strong>     <a href="/posts/new">(+ post)</a>     <a href="/about">(about)</a>   </h3>
	<% if signed_in? %>
	Signed in as <%= current_user.id %>
	<%= link_to "Sign out", signout_path, method: "delete" %>
	<% else %>
	<%= link_to "Sign in", signin_path %>
	signed out
	<% end %>
	<div>

<div class="container-fluid">
	<% flash.each do |key, value| %>
    	<div class="alert alert-<%= key %>"><%= value %></div>
    <% end %>
	<%= yield %>
	<%= debug(params) if Rails.env.development? %>
</div>

</body>
</html>
